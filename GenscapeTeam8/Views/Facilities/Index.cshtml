<div class="row">
    <div class="col-lg-12">
        <h1>Facilities</h1>
        <p class="lead">Here are the current facilities.</p>
    </div>
</div>
<div class="row">
    <div class="col-lg-12">
        <table class="table zebra-table">
            <thead>
                <tr>
                    <th>Name</th>
                    <th># of Cameras</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @if (ViewBag.Facilities.Count == 0)
                {
                    <tr>
                        <td colspan="2" class="text-center">No facilities found</td>
                    </tr>
                }
                else
                {
                    foreach (var facility in ViewBag.Facilities)
                    {
                        <tr data-facility-id="@facility.FacilityID">
                            <td>@facility.Name</td>
                            <td>@facility.Cameras.Count</td>
                            <td>@Html.ActionLink("View Cameras", "Index", "Cameras", new { id = @facility.FacilityID }, null)</td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>
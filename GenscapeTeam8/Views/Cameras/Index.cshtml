<div class="row">
    <div class="col-lg-12">
        <h1>Cameras</h1>
        <p class="lead">Here are the cameras for the application.</p>
    </div>
</div>
<div class="row">
    <div class="col-lg-12">
        <table class="table table-condensed table-striped">
            <thead>
                <tr>
                    <th></th>
                    <th>Facility Name</th>
                    <th>Camera Name</th>
                    <th># of Watch Points</th>
                    <th>Url</th>
                    <th>Path</th>
                </tr>
            </thead>
            <tbody>
                @if (ViewBag.Cameras.Count == 0)
                {
                    <tr>
                        <td colspan="6" class="text-center">No cameras found</td>
                    </tr>
                }
                else
                {
                    foreach (var camera in ViewBag.Cameras)
                    {
                        <tr data-camera-id="@camera.CameraID" data-camera-moniker="@(camera.Moniker == null ? String.Empty : @Html.Raw(camera.Moniker))">
                            <td>
                                <a href="/Cameras/Overview/@camera.CameraID" title="View Motion Graph"><span class="glyphicon glyphicon-film"></span></a>
                                <a href="/WatchPoints/Index/@camera.CameraID" title="View Watch Points"><span class="glyphicon glyphicon-search"></span></a>
                            </td>
                            <td>@(camera.Facility == null ? String.Empty : camera.Facility.Name)</td>
                            <td>@camera.Name</td>
                            <td>@camera.WatchPoints.Count</td>
                            <td>@camera.Url</td>
                            <td>@camera.Path</td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>